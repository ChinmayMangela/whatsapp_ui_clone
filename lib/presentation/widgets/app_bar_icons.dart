import 'package:flutter/material.dart';

Widget buildQRCodeIconButton() {
  return IconButton(onPressed: () {}, icon: const Icon(Icons.qr_code_scanner), color: Colors.white,);
}

Widget buildCameraIconButton() {
  return IconButton(onPressed: () {}, icon: const Icon(Icons.camera_alt_outlined), color: Colors.white,);
}

Widget buildSearchIconButton() {
  return IconButton(onPressed: () {}, icon: const Icon(Icons.search), color: Colors.white,);
}

Widget buildMoreIconButton() {
  return IconButton(onPressed: () {

  }, icon: const Icon(Icons.more_vert), color: Colors.white,);
}

Widget buildVideoCallButton() {
  return IconButton(onPressed: () {
  }, icon: const Icon(Icons.video_call), color: Colors.white,);
}

Widget buildPhoneCallButton() {
  return IconButton(onPressed: () {
  }, icon: const Icon(Icons.phone_outlined), color: Colors.white,);
}

